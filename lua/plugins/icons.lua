return {
    "echasnovski/mini.icons",
    version = "*", -- Use the latest stable version
    config = function()
        local mini_icons = require("mini.icons")
        mini_icons.setup({})
    end,
}

